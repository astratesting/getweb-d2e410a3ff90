# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=getweb
DATABASE_URL=postgresql://postgres:change_me_in_production@localhost:5432/getweb

# Auth — generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change_me_to_a_random_256_bit_hex_string

# Anthropic Claude (for AI page generation)
ANTHROPIC_API_KEY=sk-ant-...

# Google Maps Places API (for scraping)
GOOGLE_MAPS_API_KEY=AIza...

# SMTP (for sending preview emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@gmail.com
SMTP_PASSWORD=your_app_password

# App
APP_BASE_URL=http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000
